double exp(double x)
{
    // double y = x
    // if (x < 0)
    // {
    //     y = x *(-1)
    // }

    double a = 1
    double sum = 1 + (y/a)
    //double helper

    for (i=2, i < tollerance, ++i)
    {
        y *= y
        a *= i

        sum += y/a
    }



}

1 1 -1 2 1 2 1 1 -3 2 -4

int count_sum(int arr[], int size, int k)
{
    int arr2[size];
    for()arr2 = cumm_sum(arr1)
    
    int counter

    for(i = 0, i < size, ++i)
    {
        curr_sum = arr[i]
        for(j = 0, j+i < size, ++j)
        {
            curr_sum = arr[i+j]

            if curr_sum == k
            ++counter
            else if curr_sum > __k
            break
        }
    }
    return counter
}











